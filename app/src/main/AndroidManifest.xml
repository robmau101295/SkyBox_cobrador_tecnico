<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="skybox.com.scmtv">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".comun.MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/MyTheme.DayNight">
        <activity android:name=".mostrardetalle"></activity>
        <activity android:name=".buscar_cliente" />
        <activity android:name=".config_servidor" />
        <activity android:name=".UI.MainActivity2_login"
            android:theme="@style/MyTheme.DayNight"/>
        <activity android:name=".MainActivity2_collector" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>